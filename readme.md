Você vai precisar alterar seu arquivos `/hosts`. No `Ubuntu` o caminho dele é `/etc/hosts`. Tem que conter o mapeamento das instâncias do eureka.

```txt
127.0.0.1 localhost
127.0.0.1 server1
127.0.0.1 server2
127.0.0.1 server3
```